# Models

This directory stores optional trained artifacts.

Expected classifier path:

```text
models/document_classifier.pkl
```

If this file is absent, the application automatically uses the rule-based classifier from `core/document_classifier.py`.

